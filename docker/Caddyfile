{
	admin off
	auto_https off
}

:8080 {
	encode zstd gzip
	root * /srv

	# `route` preserves handler order. Without it, Caddy runs `try_files`
	# before `respond`, causing the SPA fallback to mask this health endpoint.
	route {
		@health path /healthz
		respond @health "ok" 200

		try_files {path} /index.html
		file_server
	}
}
